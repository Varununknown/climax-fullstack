services:
  - type: web
    name: climax-fullstack
    runtime: node
    buildCommand: npm install && npm run build:frontend
    startCommand: node server.cjs
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
